# Architectural Change Log

### 2026-06-16T03:18:30.148Z
**Plan:** `cf48c3d4-ffa2-4c1a-8696-4815b967951d`
**Intent:** Close four accepted super-review / arch-baseline notes (PLAN R3) in the planning MCP App, all small and behavior-preserving: (1) spotlight Azure-DevOps-sourced content in the create_approved tool result by switching its return from jsonResult to the existing externalJsonResult helper (consistent with get_context and sync); (2) unify the two duplicated work-item-type->CSS mappings in workspace.html (typeClass() class-suffix mapping and card()'s inline borderLeftColor color-var ternary) into a single TYPE_STYLE lookup table read by a small typeStyle() projection; (3) document, in index.ts, the static-registered ui:// resource URI vs the per-call live content hash — match in production, diverge only under the dev-only ADO_PLANNING_UI_PATH override; (4) make PlanningDraft.createdAt/updatedAt optional in types.ts to match schema.ts and the timestamp-free normalization. Update the registration test to assert create_approved external content is spotlighted and to parse the raw copy.
**Summary:** R3: spotlight create_approved result via externalJsonResult; unify work-item-type styling into a single TYPE_STYLE table + typeStyle(); document static ui:// URI vs per-call hash; make PlanningDraft.createdAt/updatedAt optional. Behavior-preserving; all gates green; committed 003242c.
**Files:** `src/tools/mcp-apps/planning/index.ts`, `src/tools/mcp-apps/planning/types.ts`, `src/tools/mcp-apps/planning/ui/workspace.html`, `src/tools/mcp-apps/planning/ui.ts`, `test/src/tools/mcp-apps.test.ts`
---### 2026-06-16T04:02:17.097Z
**Plan:** `a7451445-f0b3-41b9-94c6-e52a4d841453`
**Intent:** Fix a defect surfaced by the R1 live ADO create: get_context returned the wrong requirement-type hint for an Agile project (pbiTypeName "Product Backlog Item" instead of "User Story"), because context.ts only refines pbiTypeName from team backlogs and falls back to a Scrum-defaulted constant when no team is supplied. The wrong hint led the planner to create an invalid "Product Backlog Item" in an Agile project, which failed. Two changes: (1) in context.ts, derive processHints from the project's ACTUAL work item types via getWorkItemTypes(project) — team-independent — choosing the requirement type from [User Story, Product Backlog Item, Requirement, Issue] and confirming Epic/Feature/Task; remove the now-redundant team-backlog pbiTypeName inference; record a best-effort warning on failure. (2) in create-approved.ts, enrich the 'no created work item id' failure message to name the work item type and explain that it likely does not exist in the project's process (e.g. Product Backlog Item is Scrum-only; Agile uses User Story). Add/adjust unit tests in planning.context.test.ts and planning.create-approved.test.ts.
**Summary:** R6: derive get_context process hints from the project's real work item types (team-independent, Agile->User Story); enrich create-approved no-id error to name the type and process. Fixes the R1 live-create defect. 1012 tests green; committed 8dd6431.
**Files:** `src/tools/mcp-apps/planning/context.ts`, `src/tools/mcp-apps/planning/create-approved.ts`, `test/src/tools/planning.context.test.ts`, `test/src/tools/planning.create-approved.test.ts`
---### 2026-06-16T11:31:31.839Z
**Plan:** `1ddb24f1-d4d9-48ff-b21f-8605383956f3`
**Intent:** R7.1 (sizing + maximize quick-win) for the planning workspace UI. (1) In index.ts, bump the open tool's _meta.ui.preferred-frame-size hint to a larger size so hosts that honor it give the app more room. (2) In ui/workspace.html, add two in-app affordances the user controls within the host-given frame: a "Focus" toggle that expands a single pane (Backlog Map or Item Inspector) to full width and collapses the others (pure CSS class on the main grid), and a best-effort "Full screen" button that calls element.requestFullscreen() on the app root (works in hosts that permit it, harmless no-op otherwise), plus making the app reliably fill 100% of whatever frame height the host provides. Regenerate ui.ts via build:ui. True host-overlay maximize (like mermaid) needs the @mcp-ui/ext-apps capability and is deferred; this is the achievable within-frame win.
**Summary:** R7.1: bumped preferred-frame-size to 1440x960; added in-app Focus (single-pane) toggle + best-effort Full screen button. All gates green; committed 7b855f3.
**Files:** `src/tools/mcp-apps/planning/index.ts`, `src/tools/mcp-apps/planning/ui/workspace.html`, `src/tools/mcp-apps/planning/ui.ts`
---### 2026-06-16T20:35:31.054Z
**Plan:** `177375cb-f2a4-4c2f-9d80-3c81f29dd2c5`
**Intent:** R7.2: add a load_backlog capability so the workspace can pull a project's EXISTING Azure DevOps work items into a PlanningDraft for viewing/editing (not just newly-generated drafts). New module backlog.ts exports loadBacklog(connection, project, options): given project + optional team/areaPath/wiql/top/ids, query ADO (queryByWiql when ids absent, else use ids) then getWorkItemsBatch for fields, and map each item into a DraftWorkItem (localId 'wi-<adoId>', adoId, url, type, title, description [HTML stripped to text], state, parentLocalId 'wi-<parentId>' when the parent is in the loaded set, areaPath, iterationPath, assignedTo [identity uniqueName/displayName], tags [split System.Tags], status 'created'). Items whose type is not in SUPPORTED_TYPES are skipped with a reason; acceptance-criteria HTML is noted but not imported (fidelity limitation). Returns { draft, warnings, skipped }. Register a new tool mcp_ado_app_planning_load_backlog in index.ts that wraps the result with externalJsonResult (ADO-sourced, spotlighted) and add a zod loadBacklogOptionsSchema in schema.ts. Reuse the typed WebApi client (no REST duplication), mirroring sync.ts. Add unit tests in a new planning.backlog.test.ts.
**Summary:** R7.2: load_backlog tool + backlog.ts loadBacklog() maps existing ADO items into a PlanningDraft (WIQL/ids, parent-in-set linkage, status created). Code-review fixes: cap ids path, skip null ids. 1017 tests green; committed f918b7f.
**Files:** `src/tools/mcp-apps/planning/backlog.ts`, `src/tools/mcp-apps/planning/schema.ts`, `src/tools/mcp-apps/planning/index.ts`, `test/src/tools/planning.backlog.test.ts`, `test/src/tools/mcp-apps.test.ts`
---### 2026-06-16T20:48:02.513Z
**Plan:** `60d02700-d0ae-406c-92ef-2bcacbea6e18`
**Intent:** R7.3: add an update_items write-back path so users can edit EXISTING/created work items and save changes to Azure DevOps. New module update.ts exports updateWorkItems(connection, project, items, options): for each input item with an adoId, build a JSON-patch of only the provided/changed fields (title, description+AC via encodeFormattedValue + multilineFieldsFormat, state, areaPath, iterationPath, assignedTo, tags) and call workItemApi.updateWorkItem with validateOnly=true when options.dryRun is set (ADO validates without persisting — the dry-run-before-write guarantee), else persist. Per-item failures are isolated into a failed[] array, never aborting the batch; items without an adoId are skipped. Returns { dryRun, updated, skipped, failed }. Register a new tool mcp_ado_app_planning_update_items in index.ts wrapping the result with externalJsonResult, and add a zod updateItemsSchema in schema.ts. This EXTENDS the existing human-in-the-loop write contract (dry-run-before-write, per-item isolation) from creation to updates — same safety invariants, mirroring create-approved.ts.
**Summary:** R7.3: update_items write-back tool (dryRun=validateOnly, per-item isolation, lazy API). Extracted shared field-ops.ts used by both create-approved and update (consolidates patch-construction; baseline risk-3 partially mitigated). 1022 tests green; committed a11c2c2.
**Files:** `src/tools/mcp-apps/planning/update.ts`, `src/tools/mcp-apps/planning/field-ops.ts`, `src/tools/mcp-apps/planning/create-approved.ts`, `src/tools/mcp-apps/planning/types.ts`, `src/tools/mcp-apps/planning/schema.ts`, `src/tools/mcp-apps/planning/index.ts`, `test/src/tools/planning.update.test.ts`, `test/src/tools/mcp-apps.test.ts`
---### 2026-06-16T21:00:01.130Z
**Plan:** `0f714dd2-deb6-4f17-a008-0d1bce763028`
**Intent:** R7.6 (light persistence) in ui/workspace.html: persist the working PlanningDraft to the browser's localStorage so the view/modify loop survives closing and reopening the workspace, and restore it on load. Add persist() (writes JSON.stringify(state.draft) under a fixed key) and restore() (reads + JSON.parse), both wrapped in try/catch so a sandboxed host with no localStorage silently no-ops. Call persist() at the end of render() so any mutation is saved. On init, if no draft was bootstrapped and a stored draft exists, loadDraft() it and log a 'restored' message. Clear Draft also removes the stored key. Reload-from-ADO is already covered by the load_backlog button (R7.4). Vanilla JS in the self-contained workspace.html; regenerate ui.ts via build:ui.
**Summary:** R7.6: localStorage persist/restore of the working draft (best-effort); survives reopen. Committed 50676c0. Completes R7 (backlog round-trip + UI redesign).
**Files:** `src/tools/mcp-apps/planning/ui/workspace.html`, `src/tools/mcp-apps/planning/ui.ts`
---### 2026-06-17T15:13:13.330Z
**Plan:** `85396f82-80e8-4137-a1c7-469eb1fe062d`
**Intent:** R7.7: add a "Run skill on this work item" control to the planning workspace cards. In ui/workspace.html, each card's action row gets a compact "Run skill…" dropdown (placeholder first option, then a configurable skill list); choosing a skill calls a new runSkillOnItem(item, skill) that builds a message `Run: <skill> on <project> #<adoId|localId> — "<title>"` followed by the item's content (title/description/acceptance criteria/state/area) wrapped by a new spotlightText(text, source) helper (random-nonce delimiters, mirroring the server's content-safety spotlighting) so the skill treats the untrusted ADO content as data, not instructions. It emits the prompt to the host via an mcp-ui style prompt action (postMessage {type:"prompt", payload:{prompt}}) and a sendPrompt() global if present, and ALWAYS copies the command to the clipboard as a reliable fallback (logs the result). "Other…" lets the user type a custom slash-command. The skill list comes from window.__ADO_PLANNING_BOOTSTRAP__.skills when present else a curated default; in index.ts the open tool's bootstrap is extended to include skills read from process.env.ADO_PLANNING_SKILLS (comma-separated) when set. Regenerate ui.ts via build:ui.
**Summary:** R7.7: card "Run skill" control emits an mcp-ui prompt action (+ clipboard fallback) with a 128-bit-spotlight-wrapped work-item payload; configurable skill list via ADO_PLANNING_SKILLS env. 1022 tests green; committed 276b816.
**Files:** `src/tools/mcp-apps/planning/ui/workspace.html`, `src/tools/mcp-apps/planning/ui.ts`, `src/tools/mcp-apps/planning/index.ts`
---