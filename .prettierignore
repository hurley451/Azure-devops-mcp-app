node_modules/

dist/
coverage/

*.log*

# Nested git worktrees / local session state — never format another checkout's tree.
# (The `format` script is `prettier --write .`; without this, a commit from the parent
# checkout reformats every worktree under here, churning their generated/ignored files.)
.claude/

# Planning MCP App UI: workspace.html is the hand-edited source, ui.ts is generated from it.
# Prettier would reformat the embedded JS/CSS and churn the content hash.
src/tools/mcp-apps/planning/ui/workspace.html
src/tools/mcp-apps/planning/ui.ts

# arch-guardrail-mcp baseline: tool-generated, machine-parsed report/scorecard/persona files.
# Prettier reformatting would corrupt the keyed parser and churn on every regeneration.
.architectural-review/
